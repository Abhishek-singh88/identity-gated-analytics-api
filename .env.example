# Create a .env file in the root of your project and copy the following content into it. 

NODE_ENV=development
PORT=3000
JWT_SECRET=change-me    
REDIS_URL=redis://localhost:6379
INJ_NETWORK=mainnet
DEV_BYPASS_NFT=true      # Set to true to bypass NFT checks for testing purposes
DEV_BYPASS_NFT_TIER=nftHolder
DEV_BYPASS_SIGNATURE=true    # Set to true to bypass signature verification for testing purposes
DEV_BYPASS_NFT_TIER=premium    # Set the NFT tier premium to bypass for testing


